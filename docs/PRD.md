# SocialMedia-Hub 产品需求文档 (PRD)

## 文档信息

| 属性 | 值 |
|------|-----|
| **产品名称** | SocialMedia-Hub |
| **版本** | v0.1.0 |
| **文档版本** | 1.0 |
| **创建日期** | 2026-07-06 |
| **最后更新** | 2026-07-06 |
| **作者** | SocialMedia-Hub Team |

---

## 1. 产品概述

### 1.1 产品定位

SocialMedia-Hub 是一个**高性能、多平台社交媒体数据 API 服务**，提供统一的 REST API 接口，支持 16+ 社交媒体平台的数据访问，包括抖音、TikTok、Instagram、YouTube、Twitter/X、小红书、B站、微博等。

### 1.2 目标用户

| 用户类型 | 使用场景 |
|----------|----------|
| **开发者** | 构建社交媒体数据分析应用 |
| **数据科学家** | 收集社交媒体数据用于机器学习训练 |
| **AI 工程师** | 为 LLM 提供社交媒体上下文数据 |
| **营销团队** | 监测品牌舆情、竞品分析 |
| **内容创作者** | 分析内容表现、粉丝画像 |
| **研究人员** | 社交媒体趋势研究、学术分析 |

### 1.3 核心价值

1. **统一接口** — 一个 API Key 访问 16+ 平台
2. **1010+ 端点** — 覆盖各平台核心功能
3. **实时数据** — 视频、用户、评论、搜索、直播等
4. **生产级质量** — 类型安全、自动重试、结构化异常
5. **开箱即用** — SDK + CLI + API Server 三件套

---

## 2. 功能需求

### 2.1 平台覆盖

| 平台 | 端点数 | 核心功能 |
|------|--------|----------|
| **抖音 (Douyin)** | 234 | 视频、用户、评论、搜索、直播、电商、星图、创作者 |
| **TikTok** | 150+ | 视频、用户、评论、搜索、广告、电商、创作者、分析 |
| **Instagram** | 88 | 用户、帖子、Reels、Story、探索、话题 |
| **小红书 (Xiaohongshu)** | 79 | 笔记、用户、评论、搜索、话题、商品 |
| **微博 (Weibo)** | 64 | 微博、用户、评论、搜索、热搜 |
| **YouTube** | 40 | 视频、频道、播放列表、评论、搜索、Shorts |
| **B站 (Bilibili)** | 41 | 视频、用户、评论、弹幕、直播、动态 |
| **知乎 (Zhihu)** | 34 | 问题、回答、文章、用户、话题、搜索 |
| **快手 (Kuaishou)** | 33 | 视频、用户、评论、搜索、直播 |
| **Reddit** | 26 | 帖子、评论、子版块、用户 |
| **LinkedIn** | 25 | 用户、帖子、公司、职位、搜索 |
| **Twitter/X** | 13 | 推文、用户、搜索、热搜 |
| **微信 (WeChat)** | 20 | 视频号、公众号、文章 |
| **Threads** | 11 | 用户、帖子、评论 |
| **Lemon8** | 16 | 帖子、用户、搜索、分类 |
| **其他** | 30+ | 皮皮虾、Sora2、西瓜视频、头条等 |

### 2.2 API 功能分类

#### 2.2.1 内容获取

| 功能 | 说明 | 支持平台 |
|------|------|----------|
| 获取单个视频/帖子 | 通过 ID 获取内容详情 | 全部 |
| 获取用户发布内容 | 获取用户的作品列表 | 全部 |
| 获取推荐内容 | 获取首页推荐流 | 抖音、TikTok、小红书 |
| 获取相关推荐 | 获取相关推荐内容 | 抖音、TikTok、YouTube |
| 获取热门内容 | 获取热搜榜/热榜 | 全部 |

#### 2.2.2 用户信息

| 功能 | 说明 | 支持平台 |
|------|------|----------|
| 获取用户资料 | 获取用户基本信息 | 全部 |
| 获取用户粉丝列表 | 获取粉丝列表 | 抖音、TikTok、小红书等 |
| 获取用户关注列表 | 获取关注列表 | 抖音、TikTok、小红书等 |
| 获取用户动态 | 获取用户动态/时间线 | 抖音、B站、微博 |
| 批量获取用户 | 批量获取多个用户信息 | 抖音、TikTok |

#### 2.2.3 评论系统

| 功能 | 说明 | 支持平台 |
|------|------|----------|
| 获取评论列表 | 获取内容评论 | 全部 |
| 获取评论回复 | 获取子评论 | 全部 |
| 获取评论点赞 | 获取评论点赞用户 | Instagram、Twitter |

#### 2.2.4 搜索功能

| 功能 | 说明 | 支持平台 |
|------|------|----------|
| 综合搜索 | 搜索视频/帖子 | 全部 |
| 用户搜索 | 搜索用户 | 全部 |
| 话题搜索 | 搜索话题/标签 | 抖音、小红书、知乎 |
| 直播搜索 | 搜索直播 | 抖音、TikTok |
| 商品搜索 | 搜索商品 | 抖音、TikTok Shop |

#### 2.2.5 直播功能

| 功能 | 说明 | 支持平台 |
|------|------|----------|
| 获取直播间信息 | 获取直播间详情 | 抖音、TikTok、快手 |
| 获取直播视频流 | 获取直播流地址 | 抖音、B站 |
| 获取直播弹幕 | 获取直播弹幕 | 抖音、B站 |
| 获取直播礼物排行 | 获取礼物排行榜 | 抖音 |

#### 2.2.6 电商功能

| 功能 | 说明 | 支持平台 |
|------|------|----------|
| 获取商品详情 | 获取商品信息 | 抖音、TikTok Shop |
| 获取商品SKU | 获取商品规格 | 抖音 |
| 获取商品评价 | 获取商品评价 | 抖音、TikTok Shop |
| 获取订单列表 | 获取订单信息 | TikTok Shop |

#### 2.2.7 创作者工具

| 功能 | 说明 | 支持平台 |
|------|------|----------|
| 获取创作者数据 | 获取创作者分析数据 | 抖音、TikTok |
| 获取视频数据 | 获取视频统计数据 | 抖音、TikTok |
| 获取粉丝数据 | 获取粉丝画像 | 抖音、TikTok |
| 获取内容数据 | 获取内容表现数据 | 抖音、TikTok |

#### 2.2.8 广告与营销

| 功能 | 说明 | 支持平台 |
|------|------|----------|
| 获取广告活动 | 获取广告活动列表 | TikTok Ads |
| 获取广告报告 | 获取广告数据报告 | TikTok Ads |
| 获取受众列表 | 获取受众信息 | TikTok Ads |
| 获取星图数据 | 获取达人数据 | 抖音星图 |

#### 2.2.9 工具类

| 功能 | 说明 | 支持平台 |
|------|------|----------|
| URL 解析 | 解析短链接获取内容ID | 抖音、TikTok |
| ID 转换 | 不同ID格式转换 | 抖音、B站 |
| 签名生成 | 生成请求签名 | 抖音、TikTok |
| Token 生成 | 生成访问令牌 | 抖音、TikTok |
| 验证码解决 | 解决验证码 | 全部 |

### 2.3 SDK 功能

#### 2.3.1 客户端

| 功能 | 说明 |
|------|------|
| 同步客户端 | `SocialMediaHub` - 同步 API 调用 |
| 异步客户端 | `AsyncSocialMediaHub` - 异步 API 调用 |
| 上下文管理器 | 支持 `with` 语句自动关闭连接 |
| 配置灵活 | 支持超时、代理、自定义 HTTP 客户端 |

#### 2.3.2 可靠性

| 功能 | 说明 |
|------|------|
| 自动重试 | 指数退避重试（5xx、429、网络错误） |
| 限流处理 | 基于 `X-RateLimit-Reset` 的限流感知 |
| 超时处理 | 可配置的请求超时 |
| 错误分类 | 结构化异常层次（15 种异常类型） |

#### 2.3.3 类型安全

| 功能 | 说明 |
|------|------|
| mypy --strict | 全部通过类型检查 |
| pydantic v2 | 数据模型验证 |
| 参数类型 | 所有参数都有类型注解 |

### 2.4 CLI 功能

| 命令 | 说明 |
|------|------|
| `smh version` | 显示版本号 |
| `smh health` | 检查服务器健康状态 |
| `smh fetch <url>` | 解析社交媒体 URL |
| `smh server start` | 启动 API 服务器 |
| `smh server status` | 检查服务器状态 |
| `smh create-key` | 创建 API Key |

### 2.5 API Server 功能

| 功能 | 说明 |
|------|------|
| FastAPI 服务器 | 高性能异步 API 服务 |
| API Key 认证 | 基于 Bearer Token 的认证 |
| 限流控制 | 基于时间窗口的限流 |
| CORS 支持 | 跨域请求支持 |
| 通用代理 | 支持任意平台的通用代理路由 |
| 自动路由注册 | 从自动生成的路由文件动态注册 |

---

## 3. 非功能需求

### 3.1 性能

| 指标 | 目标 |
|------|------|
| API 响应时间 | < 500ms (P95) |
| 并发支持 | 1000+ 并发请求 |
| 启动时间 | < 2s |

### 3.2 可靠性

| 指标 | 目标 |
|------|------|
| 可用性 | 99.9% |
| 错误恢复 | 自动重试 + 降级 |
| 数据一致性 | 最终一致性 |

### 3.3 安全性

| 措施 | 说明 |
|------|------|
| API Key 认证 | 所有接口需要认证 |
| 传输加密 | HTTPS 强制 |
| 密钥脱敏 | 异常信息中自动脱敏 Authorization |
| 输入验证 | 参数类型和范围验证 |

### 3.4 可维护性

| 措施 | 说明 |
|------|------|
| 代码生成 | 从 OpenAPI spec 自动生成路由 |
| 类型安全 | mypy --strict 全部通过 |
| 测试覆盖 | 39 个单元测试 |
| 代码规范 | ruff lint 全部通过 |

---

## 4. 技术架构

### 4.1 技术栈

| 层 | 技术 |
|----|------|
| **Web 框架** | FastAPI |
| **HTTP 客户端** | httpx (同步/异步) |
| **数据验证** | pydantic v2 |
| **异步支持** | anyio |
| **CLI** | typer |
| **构建** | hatchling |
| **类型检查** | mypy --strict |
| **代码规范** | ruff |
| **测试** | pytest + pytest-asyncio |

### 4.2 架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        客户端层                              │
├─────────────────────────────────────────────────────────────┤
│  SDK (Python)          │  CLI           │  HTTP Client      │
│  SocialMediaHub        │  smh           │  curl/Postman     │
│  AsyncSocialMediaHub   │                │                   │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                     API Server (FastAPI)                     │
├─────────────────────────────────────────────────────────────┤
│  认证层          │  路由层          │  代理层               │
│  API Key 验证    │  自动路由注册    │  平台请求代理         │
│  限流控制        │  参数验证        │  响应处理             │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    社交媒体平台 API                          │
├─────────────────────────────────────────────────────────────┤
│  抖音  │ TikTok │ Instagram │ YouTube │ Twitter │ 小红书    │
│  B站   │ 微博   │ 快手      │ 知乎    │ Reddit  │ LinkedIn │
│  微信  │ Threads│ Lemon8    │ 皮皮虾  │ Sora2   │ 其他     │
└─────────────────────────────────────────────────────────────┘
```

### 4.3 代码生成流程

```
TikHub OpenAPI Spec (openapi.json)
        │
        ▼
scripts/generate_routes.py
        │
        ▼
51 个 auto_*.py 路由文件 (1010 端点)
        │
        ▼
server/main.py 动态加载注册
        │
        ▼
FastAPI 应用 (1019 个端点)
```

---

## 5. API 设计

### 5.1 通用响应格式

```json
{
  "code": 200,
  "request_id": "uuid",
  "router": "/api/v1/{platform}/{endpoint}",
  "params": {},
  "data": {},
  "time": "ISO8601",
  "time_zone": "UTC"
}
```

### 5.2 错误响应格式

```json
{
  "detail": {
    "code": 401,
    "request_id": "uuid",
    "message": "API token is missing",
    "message_zh": "API令牌缺失",
    "router": "/api/v1/{platform}/{endpoint}",
    "params": {},
    "headers": {},
    "time": "ISO8601",
    "time_stamp": 1234567890,
    "time_zone": "America/Los_Angeles"
  }
}
```

### 5.3 认证方式

```
Authorization: Bearer <API_KEY>
```

### 5.4 端点命名规则

| 规则 | 示例 |
|------|------|
| 路径格式 | `/api/v1/{platform}/web/{function}` |
| 平台标识 | 小写，如 `douyin`, `tiktok`, `instagram` |
| 接口类型 | `web` (网页端) / `app` (移动端) |
| 功能命名 | 动词_名词，如 `fetch_video`, `fetch_user_profile` |

### 5.5 分页参数

| 参数 | 类型 | 说明 |
|------|------|------|
| `cursor` | string | 游标分页 |
| `page` | int | 页码分页 |
| `offset` | int | 偏移分页 |
| `count` / `limit` | int | 每页数量 |

---

## 6. 部署方案

### 6.1 本地开发

```bash
# 安装依赖
pip install -e ".[dev]"

# 启动服务器
smh-server start --port 8000

# 运行测试
pytest -v
```

### 6.2 Docker 部署

```dockerfile
FROM python:3.12-slim
WORKDIR /app
COPY . .
RUN pip install -e ".[server]"
EXPOSE 8000
CMD ["smh-server", "start"]
```

### 6.3 云部署

| 平台 | 方式 |
|------|------|
| Vercel | Serverless Functions |
| AWS Lambda | 容器镜像 |
| Google Cloud Run | 容器镜像 |
| Azure Container Apps | 容器实例 |

---

## 7. 监控与运维

### 7.1 监控指标

| 指标 | 说明 |
|------|------|
| 请求量 | 每分钟/小时/天的请求总数 |
| 响应时间 | P50/P95/P99 响应时间 |
| 错误率 | 4xx/5xx 错误比例 |
| 并发数 | 同时处理的请求数 |
| 限流触发 | 被限流的请求数 |

### 7.2 日志

| 日志类型 | 说明 |
|----------|------|
| 访问日志 | 每个请求的详细信息 |
| 错误日志 | 异常和错误堆栈 |
| 限流日志 | 被限流的请求 |
| 重试日志 | 重试操作记录 |

### 7.3 告警

| 告警条件 | 级别 |
|----------|------|
| 错误率 > 5% | Warning |
| 错误率 > 10% | Critical |
| 响应时间 > 2s | Warning |
| 服务不可用 | Critical |

---

## 8. 测试策略

### 8.1 测试类型

| 类型 | 工具 | 覆盖率 |
|------|------|--------|
| 单元测试 | pytest | 核心模块 |
| 集成测试 | pytest-asyncio | API 端点 |
| 类型检查 | mypy --strict | 全部代码 |
| 代码规范 | ruff | 全部代码 |

### 8.2 测试用例

| 模块 | 测试数 | 覆盖内容 |
|------|--------|----------|
| 错误处理 | 15 | 异常层次、状态码映射 |
| 客户端构建 | 10 | 初始化、配置、上下文管理 |
| 重试策略 | 7 | 重试逻辑、退避算法 |
| 分页器 | 5 | Cursor/Page/Offset 分页 |
| 资源基类 | 2 | 资源初始化 |

---

## 9. 版本规划

### 9.1 v0.1.0 (当前)

- [x] 核心 SDK (同步/异步客户端)
- [x] 1010 个 API 端点
- [x] FastAPI 服务器
- [x] CLI 工具
- [x] 单元测试
- [x] 代码生成脚本

### 9.2 v0.2.0 (规划)

- [ ] 数据缓存层 (Redis)
- [ ] 用户管理系统
- [ ] 用量统计仪表板
- [ ] Webhook 支持
- [ ] 更多平台端点

### 9.3 v0.3.0 (规划)

- [ ] 实时数据推送 (WebSocket)
- [ ] 数据导出 (CSV/JSON)
- [ ] 高级搜索功能
- [ ] 数据分析 API
- [ ] 企业级功能

---

## 10. 风险与缓解

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| 平台 API 变更 | 端点失效 | 代码生成 + 快速更新 |
| 反爬检测 | 请求被拦截 | 代理池 + 请求间隔控制 |
| 限流严格 | 服务不可用 | 分布式限流 + 降级策略 |
| 数据隐私合规 | 法律风险 | 数据最小化 + 用户授权 |

---

## 11. 附录

### 11.1 术语表

| 术语 | 说明 |
|------|------|
| API Key | 用于认证的密钥 |
| Endpoint | API 端点 |
| Rate Limit | 请求频率限制 |
| Proxy | 代理服务器 |
| SDK | 软件开发工具包 |
| REST API | 表述性状态转移 API |

### 11.2 参考文档

- [TikHub API 文档](https://api.tikhub.io)
- [FastAPI 文档](https://fastapi.tiangolo.com/)
- [Pydantic 文档](https://docs.pydantic.dev/)
- [httpx 文档](https://www.python-httpx.org/)

### 11.3 变更日志

| 版本 | 日期 | 变更 |
|------|------|------|
| v0.1.0 | 2026-07-06 | 初始版本，1010 个端点 |
